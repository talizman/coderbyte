def LongestWord(sen)
  return sen.split(" ").map{ |s| s.gsub(/\W+/, "") }.max_by { |s| s.length }
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
LongestWord(STDIN.gets)  
