def LetterCapitalize(str)
  return str.split(" ").map { |s| s.capitalize }.map { |b, s| b = "", b << " #{s}"}.to_s 
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
LetterCapitalize(STDIN.gets)  
